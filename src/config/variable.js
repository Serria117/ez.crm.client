const env = {
    BASE_URL: `http://localhost:5056/api/`,
    CUSTOMER_API: {
        create: `customer/create`,
        search: `customer/search`,
        getAll: `customer/get-all`,
        update: `customer/update`,
        checkTaxCode: 'customer/check-taxCode'
    }
};

export default env;
